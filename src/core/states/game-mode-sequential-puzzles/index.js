import { STAGE_SEQUENCE } from '@shared-constants/stage-sequence';


const GameModeSequentialPuzzlesState = {
    sequence: STAGE_SEQUENCE,
    activePuzzleIndex: -1
};

export { GameModeSequentialPuzzlesState };
