import { LIGHT_THEME } from '@shared-constants/color-themes';


const PrefersColorSchemeState = {
    appearance: {
        theme: {
            current: LIGHT_THEME
        }
    }
};

export { PrefersColorSchemeState };
