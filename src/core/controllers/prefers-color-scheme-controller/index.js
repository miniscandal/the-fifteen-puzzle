import { LIGHT_THEME } from '@shared-constants/color-themes';


const PrefersColorSchemeController = {
    appearance: {
        theme: {
            current: LIGHT_THEME
        }
    }
};

export { PrefersColorSchemeController };
