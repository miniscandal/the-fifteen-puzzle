import { COLOR_THEME_ID } from '@shared-constants/dom-element-identifiers';

function getElementIconButtonColorTheme() {
    return document.getElementById(COLOR_THEME_ID);
}

export { getElementIconButtonColorTheme };
