const puzzleGridPlay = {
    playEnabled: true,
    size: 'medium',
    get puzzle() {
        return (async () => {
            try {
                const { ['puzzleA1']: puzzle } = await import('@core-constants/puzzles.js');
                console.log(puzzle);

                return puzzle;
            } catch (error) {
                console.log(error);
                return {};
            }
        })();
    }
};
